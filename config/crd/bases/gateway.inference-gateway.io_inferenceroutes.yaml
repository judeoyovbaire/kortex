---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: inferenceroutes.gateway.inference-gateway.io
spec:
  group: gateway.inference-gateway.io
  names:
    kind: InferenceRoute
    listKind: InferenceRouteList
    plural: inferenceroutes
    singular: inferenceroute
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.activeBackends
      name: Backends
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InferenceRoute is the Schema for the inferenceroutes API.
          It defines routing rules for directing inference requests to backends.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InferenceRouteSpec defines the desired state of InferenceRoute
            properties:
              costTracking:
                default: true
                description: Enable cost tracking per request
                type: boolean
              defaultBackend:
                description: Default backend if no rules match
                properties:
                  name:
                    description: Name of the InferenceBackend resource
                    type: string
                  weight:
                    default: 100
                    description: Weight for weighted routing (0-100)
                    format: int32
                    maximum: 100
                    minimum: 0
                    type: integer
                required:
                - name
                type: object
              enableLogging:
                default: false
                description: Enable request/response logging
                type: boolean
              experiments:
                description: A/B testing experiments
                items:
                  description: ABExperiment defines an A/B test configuration
                  properties:
                    control:
                      description: Control backend name
                      type: string
                    metric:
                      default: latency_p95
                      description: Metric to track for statistical analysis
                      type: string
                    name:
                      description: Name of the experiment
                      type: string
                    trafficPercent:
                      default: 10
                      description: Percentage of traffic to send to treatment (0-100)
                      format: int32
                      maximum: 100
                      minimum: 0
                      type: integer
                    treatment:
                      description: Treatment backend name
                      type: string
                  required:
                  - control
                  - name
                  - treatment
                  type: object
                type: array
              fallback:
                description: Fallback chain for automatic failover
                properties:
                  backends:
                    description: Ordered list of backend names to try
                    items:
                      type: string
                    minItems: 1
                    type: array
                  timeoutSeconds:
                    default: 30
                    description: Timeout per backend attempt in seconds
                    format: int32
                    type: integer
                required:
                - backends
                type: object
              rateLimit:
                description: Rate limiting configuration
                properties:
                  perUser:
                    default: false
                    description: Apply rate limit per user (based on header)
                    type: boolean
                  requestsPerMinute:
                    description: Maximum requests per minute
                    format: int32
                    minimum: 1
                    type: integer
                  userHeader:
                    default: x-user-id
                    description: Header name to identify users
                    type: string
                required:
                - requestsPerMinute
                type: object
              rules:
                description: Rules for routing requests to backends
                items:
                  description: RouteRule defines a single routing rule
                  properties:
                    backends:
                      description: Backends to route matching requests to
                      items:
                        description: BackendRef references a backend for routing
                        properties:
                          name:
                            description: Name of the InferenceBackend resource
                            type: string
                          weight:
                            default: 100
                            description: Weight for weighted routing (0-100)
                            format: int32
                            maximum: 100
                            minimum: 0
                            type: integer
                        required:
                        - name
                        type: object
                      minItems: 1
                      type: array
                    match:
                      description: Match conditions for this rule
                      properties:
                        headers:
                          additionalProperties:
                            type: string
                          description: Headers to match against incoming requests
                          type: object
                        modelPattern:
                          description: Model name pattern to match (supports wildcards)
                          type: string
                        pathPrefix:
                          description: Path prefix to match
                          type: string
                      type: object
                  required:
                  - backends
                  type: object
                type: array
            type: object
          status:
            description: InferenceRouteStatus defines the observed state of InferenceRoute
            properties:
              activeBackends:
                description: Active backends count
                format: int32
                type: integer
              conditions:
                description: Conditions represent the current state of the InferenceRoute
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastUpdated:
                description: Last time the route was updated
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of the route
                enum:
                - Pending
                - Active
                - Degraded
                - Failed
                type: string
              totalRequests:
                description: Total requests processed
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
