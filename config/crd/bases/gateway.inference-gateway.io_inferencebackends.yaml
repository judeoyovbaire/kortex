---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: inferencebackends.gateway.inference-gateway.io
spec:
  group: gateway.inference-gateway.io
  names:
    kind: InferenceBackend
    listKind: InferenceBackendList
    plural: inferencebackends
    singular: inferencebackend
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.health
      name: Health
      type: string
    - jsonPath: .status.totalRequests
      name: Requests
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InferenceBackend is the Schema for the inferencebackends API.
          It defines a backend service for inference requests.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InferenceBackendSpec defines the desired state of InferenceBackend
            properties:
              cost:
                description: Cost configuration for tracking
                properties:
                  currency:
                    default: USD
                    description: Currency for costs
                    type: string
                  inputTokenCost:
                    description: Cost per 1000 input tokens
                    type: string
                  outputTokenCost:
                    description: Cost per 1000 output tokens
                    type: string
                  requestCost:
                    description: Fixed cost per request
                    type: string
                type: object
              external:
                description: External API backend configuration
                properties:
                  apiKeySecret:
                    description: Secret containing the API key
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  model:
                    description: Model name to use for this backend
                    type: string
                  provider:
                    default: openai
                    description: Provider type for API compatibility
                    enum:
                    - openai
                    - anthropic
                    - cohere
                    - custom
                    type: string
                  url:
                    description: Base URL of the external API
                    type: string
                required:
                - url
                type: object
              healthCheck:
                description: Health check configuration
                properties:
                  failureThreshold:
                    default: 3
                    description: Number of consecutive failures before marking unhealthy
                    format: int32
                    type: integer
                  intervalSeconds:
                    default: 30
                    description: Interval between health checks in seconds
                    format: int32
                    type: integer
                  path:
                    default: /health
                    description: Path for health check endpoint
                    type: string
                  timeoutSeconds:
                    default: 5
                    description: Timeout for health check in seconds
                    format: int32
                    type: integer
                type: object
              kserve:
                description: KServe backend configuration
                properties:
                  namespace:
                    description: Namespace of the InferenceService (defaults to same
                      namespace)
                    type: string
                  serviceName:
                    description: Name of the KServe InferenceService
                    type: string
                required:
                - serviceName
                type: object
              kubernetes:
                description: Kubernetes Service backend configuration
                properties:
                  namespace:
                    description: Namespace of the Service (defaults to same namespace)
                    type: string
                  port:
                    default: 8080
                    description: Port to connect to
                    format: int32
                    type: integer
                  serviceName:
                    description: Name of the Kubernetes Service
                    type: string
                required:
                - serviceName
                type: object
              maxConcurrency:
                default: 100
                description: Maximum concurrent requests
                format: int32
                type: integer
              priority:
                default: 0
                description: Priority for fallback ordering (higher = preferred)
                format: int32
                type: integer
              timeoutSeconds:
                default: 60
                description: Request timeout in seconds
                format: int32
                type: integer
              type:
                description: Type of backend
                enum:
                - kserve
                - external
                - kubernetes
                type: string
            required:
            - type
            type: object
          status:
            description: InferenceBackendStatus defines the observed state of InferenceBackend
            properties:
              activeRequests:
                description: Current request count
                format: int32
                type: integer
              averageLatencyMs:
                description: Average latency in milliseconds
                format: int64
                type: integer
              conditions:
                description: Conditions represent the current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              health:
                description: Health status of the backend
                enum:
                - Healthy
                - Unhealthy
                - Unknown
                type: string
              lastHealthCheck:
                description: Last successful health check time
                format: date-time
                type: string
              totalErrors:
                description: Total errors
                format: int64
                type: integer
              totalRequests:
                description: Total requests served
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
